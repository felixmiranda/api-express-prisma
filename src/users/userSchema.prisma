model user {
    id       Int    @id @default(autoincrement())
    dni      String @unique
    email    String @unique
    name     String
    password String
    role     role   @relation(fields: [roleid], references: [id])
    roleid   Int
}
